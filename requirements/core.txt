# Core dependencies with Neuron-compatible constraints
setuptools-scm>=8
urllib3>=2.6.0

# Neuron torch compile/runtime framework
# Left unpinned to resolve based on vLLM's torch requirement
libtorch-neuronx-lite

# vLLM installation
vllm==0.24.0

# Pin <0.137: 0.137's `_IncludedRouter` breaks prometheus-fastapi-instrumentator
fastapi[standard]>=0.115.0,<0.137

# Force transformers v5 upgrade.
# Floor: 5.5.1 is vLLM 0.24's requirement.
# Ceiling: 5.15.0 is the newest version the DeepSeek-V4 config normalizer and
# component oracles have actually been exercised against
# (test/vllm_neuron/test_deepseek_v4_{config,component}_oracle.py). An open
# <6.0.0 range would let a minor release change config normalization or model
# defaults with no signal -- the drift the patch tripwires exist to catch.
# Raise the ceiling deliberately, after re-running those suites.
transformers>=5.5.1,<5.16

# Prometheus metrics
prometheus_client

# NKI CPU simulator dtype support
ml_dtypes

# NIXL KV transfer (disaggregated inference)
nixl==1.3.2

# benchmark dependency
psutil
requests
boto3
